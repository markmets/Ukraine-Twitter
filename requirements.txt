# Core
pandas>=1.5
numpy>=1.23
openpyxl>=3.1          # Excel read/write
requests>=2.28

# Storywrangler (install from GitHub if pip version is outdated)
# pip install git+https://github.com/compstorylab/storywrangling
storywrangling

# Visualisation
plotly>=5.0
kaleido>=0.2           # for saving static Plotly figures (PNG/PDF)

# NLP / analysis
shifterator
scikit-learn>=1.2      # for UMAP preprocessing / clustering
umap-learn             # for Fig.5a UMAPs
dtaidistance>=2.3      # DTW distance matrix for Fig.3/Fig.S4 clustering

# Notebook utilities
tqdm
jupyterlab
ipywidgets             # for tqdm.notebook progress bars
